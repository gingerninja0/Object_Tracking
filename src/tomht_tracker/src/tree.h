#pragma once

#include "node.h"

class Tree
{
private:
    std::vector<Node> tree_vec; // A vector of nodes, each node is the root node of a target's hypthesis tree

public:
    Tree(/* args */);
    ~Tree();
};


